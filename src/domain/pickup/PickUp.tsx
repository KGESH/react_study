import React from 'react';
import type { HomeProps } from 'types/Props';


const PickUp = ({isLoggedIn}: HomeProps) => (
    <div className='pickup'>
      This is PickUp Page
    </div>
  )

export default PickUp;